rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /faq-images/{allPaths=**} {
      allow read: if true;         // 誰でも画像を表示できる
      allow write: if true;        // 管理者URLのみ（認証は後で追加）
    }
  }
}
